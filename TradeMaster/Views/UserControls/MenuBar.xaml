<UserControl x:Class="TradeMaster.Views.UserControls.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >

    <Menu Name="Menubar" Foreground="White" Background="{StaticResource Bg_Menubar}">
        <MenuItem Header="File">
            <MenuItem Background="{StaticResource Bg_Menubar}" Header="New"/>
            <MenuItem Background="{StaticResource Bg_Menubar}" Header="Open"/>
            <MenuItem Background="{StaticResource Bg_Menubar}" Header="Save"/>
            <MenuItem Background="{StaticResource Bg_Menubar}" Header="Save as"/>
        </MenuItem>
        <MenuItem Header="Edit">
            <MenuItem Background="{StaticResource Bg_Menubar}" Header="Cut"/>
            <MenuItem Background="{StaticResource Bg_Menubar}" Header="Copy"/>
            <MenuItem Background="{StaticResource Bg_Menubar}" Header="Paste"/>
            <MenuItem Background="{StaticResource Bg_Menubar}" Header="delete"/>
        </MenuItem>
        <MenuItem Header="View"/>
        <MenuItem Header="Window"/>
        <MenuItem Header="Help"/>
    </Menu>

</UserControl>
